{% extends "base.html.twig" %}
{% block body %}
    {{ form_start(form , {'attr': {'id': 'form', 'novalidate':'novalidate'}}) }}
    {% if not form.vars.valid %}
    <div class="alert alert-warning" role="alert">
        {{ form_errors(form) }}
    </div>
        {% endif %}
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-header">Formulaire</div>
                <div class="card-body">
                    {% block form %}
                        {{ form_widget(form) }}
                    {% endblock %}
                </div>
                <div class="card-footer dark darken-1">
                    {% block action %}
                        <div class="row">
                            <div class="col-sm-12 col-md-6">
                                <a href="{{ previousUrl }}" class="mui-btn  red white-text text-lighten-4 ">
                                    <i class="fa fa-reply"></i>  Annuler</a>
                            </div>
                            <div class="col-sm-12 col-md-6 ">
                                <button type="submit" class=" float-right mui-btn  green white-text text-lighten-4 ">
                                    <i class="fa fa-check"></i>  Enregistrer</button>
                            </div>
                        </div>
                    {% endblock %}
                </div>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}